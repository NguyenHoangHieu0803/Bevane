services:
  - type: web
    name: bevane
    runtime: node
    buildCommand: npm install --omit=dev && npm rebuild better-sqlite3 --build-from-source
    startCommand: node server.js
    healthCheckPath: /api/users
    envVars:
      - key: NODE_ENV
        value: production
